def lower_bound(VA, VB, FB):
    # Input:
    # VA: #VA by 3 matrix with vertex positions in mesh A
    # FA: #FA by 3 matrix with vertex indices for each triangle in mesh A
    # FB: #FB by 3 matrix with vertex indices for each triangle in mesh B

    # Output:
    # d_AB: vector of lower bounds for the one sided h(A,B)
    num_A_vertices = 0
    return
